services:
  dev:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    working_dir: /app
    command: sh -c "pnpm install && pnpm dev --host"

volumes:
  node_modules:
